import { Carousel1Directive } from './carousel1.directive';

describe('Carousel1Directive', () => {
  it('should create an instance', () => {
    // const directive = new Carousel1Directive();
    // expect(directive).toBeTruthy();
  });
});
